package daemon

import (
	"context"
	"time"
)

type EmptyWorker struct {
}

type ITask interface {
	GetID(ctx context.Context) string
	Do(ctx context.Context, srv ds.IService) error
}

func (w *EmptyWorker) GetTasks(ctx context.Context, lastJobState any) ([]ITask, error) {
	return nil, nil
}
